# BeeChat Roadmap

## 一、最终目标

BeeChat 的最终目标是成为面向企业内网私有化部署的协同消息底座，覆盖团队沟通、频道协作、私信、线程、文件、安全审计、组织集成与企业内部系统通知。

长期形态：

```text
企业内网 IM + 协作消息中心 + 审计安全底座 + 企业系统通知入口
```

核心原则：

- 私有化部署优先。
- 数据本地可控。
- 权限与审计优先。
- 先稳定可用，再逐步工程化、平台化。
- 不追求复杂社交功能，优先服务企业协作效率。

## 二、当前所处阶段

当前处于：

```text
阶段 6.5：主聊天体验打磨与前端结构收敛已完成
```

已具备：

- 可运行 MVP。
- 登录、频道、私信、消息、线程、文件、搜索、审计、成员管理。
- SQLite 持久化。
- WebSocket 实时刷新。
- 数据库初始化迁移。
- 冒烟测试。
- Vite React TypeScript 前端。
- 统一 API Client 与类型模型。
- Playwright 回归脚本。
- Pencil 设计源文件重建。
- 需求、设计、接口、部署、迭代文档归档。

当前短板：

- `ChatWorkspace` 已拆分主聊天区、搜索、草稿、线程、设置、发现频道和管理后台逻辑，主容器已控制在 500 行以内。
- `ChatWorkspace.css` 已拆出主区、消息列表、输入区、侧边栏、右侧面板、弹层和管理后台样式，当前从 1421 行降到 92 行。
- UI/UX 细节测试已覆盖一屏布局、长文本、输入区状态、右侧面板、侧边栏搜索、窄屏和管理弹层。
- 管理后台已具备角色、安全策略、审计导出第一版。
- 审计已支持类型、操作者、关键词、起止日期组合筛选。
- 文件上传大小与扩展名已支持后台策略配置。
- IP 白名单网络策略已支持后台配置。
- 角色矩阵已新增审计员，并收敛普通成员审计访问。
- 角色矩阵已新增频道管理员，并开放频道级成员与说明维护。
- 数据库仍是 SQLite，不适合正式多人生产。
- WebSocket 仍是单实例模型。
- 阶段 4 无头 E2E 验收已通过。

## 三、路线图总览

| 阶段 | 名称 | 目标 | 状态 |
| --- | --- | --- | --- |
| 0 | 项目纠偏与归档 | 明确 BeeChat 独立项目边界 | 已完成 |
| 1 | MVP 功能底座 | 建立可运行、可演示、可迭代底座 | 已完成 |
| 2 | 设计重建与前端同步 | 重建设计源并同步主界面体验 | 主体完成 |
| 3 | 前端工程化 | 迁移到组件化前端架构 | 主体完成 |
| 4 | 企业安全与管理 | 补齐管理后台、权限、审计 | 已验收 |
| 5 | 协作能力增强 | 提升消息、文件、搜索、提醒效率 | 主体完成 |
| 6 | 生产级架构 | 支持 PostgreSQL、Redis、对象存储 | 进行中 |
| 6.5 | 主聊天体验打磨 | 主工作台 UI/UX 与前端结构收敛 | 已完成 |
| 7 | 企业系统集成 | 接入 SSO、组织、Webhook、Bot | 待启动 |
| 8 | 平台化扩展 | 形成企业消息平台能力 | 规划中 |

## 四、阶段 0：项目纠偏与归档

状态：已完成。

目标：

- 将 BeeChat 从误放的 `bee-geo` 相关上下文中纠偏。
- 所有 BeeChat 产出统一放入 `bee-chat/`。
- 文档、设计、代码、部署文件分区清晰。

已交付：

- `docs/beechat/`
- `design/beechat/`
- `docs/beepilot/`
- `docs/README.md`
- `design/README.md`

验收标准：

- BeeChat 与 BeePilot、BeeGeo 文档边界清楚。
- 后续产物有固定归档路径。

## 五、阶段 1：MVP 功能底座

状态：已完成。

目标：

- 快速形成可运行的聊天协作闭环。
- 覆盖后续迭代所需核心数据模型。
- 支持基础演示和接口验证。

已交付：

- 登录与会话。
- 频道与私信。
- 消息发送、编辑、撤回。
- 线程回复。
- 文件上传下载。
- 消息搜索。
- 未读数。
- 成员管理。
- 操作审计。
- 敏感词提示。
- WebSocket 实时刷新。
- SQLite 持久化。
- 初始化迁移。
- 冒烟脚本。

验收标准：

```text
npm run smoke
```

通过。

## 六、阶段 2：设计重建与前端同步

状态：主体完成。

目标：

- 解决原设计源文件丢失问题。
- 建立新的 BeeChat 正式设计源。
- 将前端视觉与设计稿对齐。

已交付：

- `design/beechat/beechat-main-chat.pen`
- `design/beechat/DESIGN_SPEC.md`
- `docs/beechat/DESIGN.md`
- 发现频道弹层。
- 前端视觉同步。
- 前端动态内容 HTML 转义。

待完成：

- 浏览器截图验收。
- 移动端细节优化。
- 设计评审图导出。
- 设置页第一期已实现。

阶段验收：

- Pencil 文件可打开且包含五个 BeeChat 画板。
- 主界面、登录页、线程页、发现频道符合设计规范。
- 冒烟测试通过。

## 七、阶段 3：前端工程化

状态：主体完成，持续完善。

目标：

- 从原生 HTML/CSS/JS 迁移到可维护的组件化前端。
- 为后续管理后台、设置页、复杂交互打基础。

建议技术路线：

```text
Vite + React
TypeScript
组件化 CSS 或 CSS Modules
轻量状态管理
统一 API Client
```

备选路线：

```text
Vite + Vue 3
TypeScript
Pinia
```

关键任务：

- 拆分页面与组件。
- 建立设计令牌。
- 抽象通用组件。
- 建立 API Client。
- 建立页面路由。
- 引入基础前端测试。

核心交付物：

- `frontend/`。
- 组件库基础。
- 页面级布局。
- 前端构建流程。

已交付：

- `frontend/src/api/`
- `frontend/src/components/`
- `frontend/src/features/`
- `frontend/src/types/`
- `frontend/src/styles/`
- `tests/e2e/beechat.regression.spec.js`

阶段验收：

- 功能与当前 MVP 等价。
- 样式与 Pencil 主画板一致。
- 冒烟测试通过。

## 八、阶段 4：企业安全与管理

状态：已验收。

目标：

- 将 BeeChat 从协作 MVP 推进到企业可管可控版本。

第一期已交付：

- 管理后台。
- 用户管理。
- 频道管理。
- 审计详情。

第二期已交付：

- 角色权限。
- 审计导出。
- 登录失败锁定。
- 密码策略。

第三期已交付：

- 审计组合筛选。
- 文件上传策略。
- 审计员角色。
- 普通成员审计访问收敛。
- 频道管理员角色。
- IP 白名单网络策略。

后续增强任务：

- 频道管理员按频道单独授权。

核心交付物：

- 管理后台页面。
- 管理员权限 API。
- 审计查询 API。
- 安全策略配置。

阶段验收：

- 普通用户无法访问管理能力。
- 管理员可查看关键审计。
- 高风险操作均有审计记录。

## 九、阶段 5：协作能力增强

状态：主体完成，第一期已完成频道公告与置顶消息主链路，第二期已完成消息收藏与表情回应主链路，第三期已完成图片预览与频道文件列表主链路，第四期已完成 @ 人提醒与本地草稿主链路，第五期已完成搜索结果定位主链路，第六期已完成在线状态优化主链路。

目标：

- 提升日常团队协作效率，让 BeeChat 更接近真实企业 IM 使用体验。

关键任务：

- @ 人提醒。已完成第四期。
- 频道公告。已完成第一期。
- 置顶消息。已完成第一期。
- 消息收藏。已完成第二期。
- 表情回应。已完成第二期。
- 草稿。已完成第四期。
- 图片预览。已完成第三期。
- 文件列表。已完成第三期。
- 搜索结果定位。已完成第五期。
- 在线状态优化。已完成第六期：会话动态在线、登出释放在线状态、实时在线事件、快速回归。

核心交付物：

- 消息增强 API。
- 频道公告 API。
- 消息置顶 API。
- 消息收藏 API。
- 表情回应 API。
- 频道文件列表 API。
- @ 人提醒识别与未读计数。
- 本地草稿保存。
- 文件中心。
- 搜索结果上下文定位。
- 在线状态实时同步。
- 提醒与未读机制增强。

阶段验收：

- 用户可围绕频道完成日常协作。
- 文件、搜索、线程形成闭环。

## 十、阶段 6：生产级部署架构

状态：进行中，第一期已完成健康检查增强、运行指标接口和本地备份脚本，第二期已完成结构化日志与日志落盘，第三期已完成生产部署配置骨架，第四期已完成 PostgreSQL 适配计划与表结构迁移骨架，第五期已完成 PostgreSQL 一次性导入 SQL 生成脚本，第六期已完成数据库运行模式保护，第七期已完成 PostgreSQL 查询适配层第一版，第八期已完成业务仓储契约第一版，第九期已完成主服务低风险只读路径接入 SQLite 仓储，第十期已完成频道消息分页只读路径仓储接入，第十一期已完成私信分页只读路径仓储接入，第十二期已完成审计查询只读路径仓储接入，第十三期已完成 PostgreSQL 频道文件列表只读查询，第十四期已完成频道消息与线程回复发送写入路径接入 SQLite 仓储，第十五期已完成私信发送写入路径接入 SQLite 仓储，第十六期已完成消息编辑与消息撤回写入路径接入 SQLite 仓储。

目标：

- 从单机 MVP 升级为可生产部署架构。

关键任务：

- PostgreSQL 适配。已完成第十六期：`migrations/postgres/001_init.sql`、`docs/beechat/DATABASE_PLAN.md`、`scripts/export-postgres-seed.js`、`scripts/check-database-runtime.js`、`server/database/postgres-adapter.js`、`scripts/check-postgres-adapter.js`、`server/repositories/`、`scripts/check-repository-contract.js`，覆盖表结构、索引、环境变量、迁移路径、导入 SQL 生成、运行模式保护、查询适配层、业务仓储契约、低风险只读路径接入、频道消息分页仓储接入、私信分页仓储接入、频道文件列表 PostgreSQL 查询、审计查询仓储接入、频道消息发送写入 SQLite 仓储接入、私信发送写入 SQLite 仓储接入、消息编辑与消息撤回写入 SQLite 仓储接入、PostgreSQL 消息写入显式未启用保护和回滚策略。
- Redis 会话。
- Redis Pub/Sub 或 Stream 支撑 WebSocket 多实例广播。
- 对象存储适配。
- 反向代理配置。
- 生产 Compose。已完成第三期：多阶段 Docker 构建、生产 Compose、环境变量样例、数据卷、日志卷、备份卷、健康检查和容器日志轮转。
- Nginx 反向代理样例。已完成第三期：普通 HTTP 代理与 WebSocket 升级配置。
- 备份恢复。已完成第一期：本地 SQLite、JSON 快照、上传文件备份脚本。
- 健康检查。已完成第一期：返回启动时间、运行时长和迁移状态。
- 运行指标。已完成第一期：管理员和审计员可查看进程、内存、会话、实时连接、数据大小和业务计数。
- 结构化日志。已完成第二期：JSON Lines 文件日志、HTTP 访问日志、异常日志、审计日志、启动日志、迁移日志和 5MB 自动轮转。
- 数据库迁移增强。

核心交付物：

- `docker-compose.prod.yml`
- `.env.prod.example`
- `deploy/nginx/beechat.conf`
- `migrations/postgres/001_init.sql` PostgreSQL schema。
- `docs/beechat/DATABASE_PLAN.md` 数据库适配方案。
- `scripts/export-postgres-seed.js` PostgreSQL 一次性导入 SQL 生成脚本。
- `scripts/check-database-runtime.js` 数据库运行模式保护检查。
- `server/database/postgres-adapter.js` PostgreSQL 查询适配层。
- `scripts/check-postgres-adapter.js` PostgreSQL 查询适配层检查。
- `server/repositories/` 业务仓储契约与双实现骨架。
- `scripts/check-repository-contract.js` 仓储契约检查。
- 主服务频道、成员、频道消息分页、私信分页、频道文件列表、审计查询只读路径、频道消息发送写入路径、私信发送写入路径、消息编辑写入路径和消息撤回写入路径接入仓储。
- Redis 集成。
- 对象存储配置。
- `/api/metrics` 运维指标接口。
- `npm run backup` 本地备份脚本。
- `logs/beechat.log` 结构化日志。
- 运维手册。

阶段验收：

- 支持单机生产部署。
- 支持后续水平扩展。
- 数据备份与恢复可演练。

## 十一、阶段 6.5：主聊天体验打磨与前端结构收敛

状态：已完成。第十五期阶段验收已通过，14 个无头 E2E 用例全部通过。

目标：

- 暂停继续堆叠大型后端能力，优先把主聊天工作台打磨到可长期演示、可持续迭代的企业 IM 体验。

第一期交付范围：

- 主聊天区拆出 `ChatMainPanel`，`ChatWorkspace` 回归容器职责。
- 主工作台保持一屏内完成核心操作，侧栏、主区、右侧面板不撑破视口。
- 频道头部、安全提示、频道公告、置顶摘要压缩视觉层级，减少消息区挤压。
- 消息长文本、文件卡片、线程根消息自动换行，不撑破消息区。
- 自己消息右对齐，其他人消息左对齐，消息操作按钮常态弱化、悬停增强。
- 输入区固定底部，发送、上传、草稿状态层级更清晰。
- 右侧面板 Tab 固定顶部，线程、成员、文件、审计内容独立滚动。
- 私信消息不展示线程入口。
- 补充 UI/UX 细节 E2E 断言，但开发期只跑快速回归。

第二期交付范围：

- 侧边栏频道与私信条目统一 `sidebar-nav-item` 结构。
- 频道未读与 `@我` 标识统一为 `nav-badge`，视觉层级更稳定。
- 私信接入 `directUnreadCounts`，支持展示私信未读徽标。
- 搜索结果增加标题栏、结果数量、加载态、空状态、错误态和悬停/聚焦反馈。
- 搜索结果与侧边栏都保持独立滚动，不撑破一屏布局。
- 补充侧边栏搜索和状态稳定性 E2E 断言。

后续分期：

- 第三期：管理后台表单、审计筛选、角色与安全策略分区优化。已完成管理后台摘要头、指标区、策略网格、审计列表和导出入口视觉增强。
第四期交付范围：

- 窄屏侧栏改为内部滚动，避免搜索、频道、当前用户区被裁切。
- 窄屏主消息区保持输入区在视口内。
- 窄屏隐藏右侧面板，主流程继续可操作。
- 通用弹层在窄屏下改为近全屏布局，管理后台可滚动查看。
- 补充窄屏侧栏、输入区、管理弹层 E2E 断言。
- 阶段验收执行无头 E2E，14 个用例全部通过。

第五期交付范围：

- 消息加载失败时在消息列表内展示错误态和重试按钮。
- 发送失败、上传失败固定展示在输入区上方，不挤占消息列表语义。
- 发送按钮在空内容、发送中、上传中状态下保持明确禁用逻辑。
- 输入区补充“待输入”“上传中”状态徽标，草稿提示继续弱化为辅助状态。
- 开发期只执行快速回归，阶段验收时再执行完整 E2E。

第六期交付范围：

- 将频道头部、安全提示、频道公告样式拆入 `ChatMainPanel.css`。
- 将消息列表、消息气泡、文件卡片、消息反应和消息错误态样式拆入 `MessageList.css`。
- 将输入区、附件入口、@ 人快捷入口和输入区错误态样式拆入 `MessageComposer.css`。
- `ChatWorkspace.css` 继续保留工作台布局、侧边栏、右侧面板、弹层和管理后台样式。
- 不改变接口语义，不新增大型后端能力。

第七期交付范围：

- 将品牌、全局搜索、搜索结果、频道、私信和当前用户卡片样式拆入 `ChatSidebar.css`。
- `ChatWorkspace.css` 继续保留工作台网格、主区壳、右侧面板、弹层和管理后台样式。
- 侧边栏原有测试定位、搜索状态、频道未读、`@我`、私信在线状态保持不变。
- 开发期只执行快速回归，不执行可视化 E2E。

第八期交付范围：

- 将右侧面板壳层、线程、成员、文件和审计样式拆入 `RightPanel.css`。
- `RightPanel.tsx` 独立引入右侧面板样式，继续保留 Tab 切换壳层职责。
- `ChatWorkspace.css` 继续收敛为工作台网格、主区壳、弹层和管理后台样式。
- 线程、成员、文件、审计面板原有测试定位和接口语义保持不变。
- 开发期只执行快速回归，不执行可视化 E2E。

第九期交付范围：

- 将发现频道弹层样式拆入 `DiscoverChannelsModal.css`。
- 将设置弹层样式拆入 `SettingsModal.css`。
- 将管理后台样式拆入 `AdminModal.css`，包含管理后台窄屏响应式规则。
- `ChatWorkspace.css` 收敛为工作台网格、主区壳和窄屏主工作台布局规则。
- 发现频道、设置、管理后台原有测试定位和接口语义保持不变。
- 开发期只执行快速回归，不执行可视化 E2E。

第十期交付范围：

- 输入区增加 `empty`、`ready`、`sending`、`uploading` 状态标记。
- 输入区表单增加 `aria-busy`，状态栏增加 `aria-live`。
- 上传入口改为固定尺寸工具按钮，提供稳定测试定位。
- 发送或上传中锁定输入框，降低重复提交和状态错乱风险。
- E2E 脚本补充输入区状态断言，开发期只做脚本语法检查。
- 开发期只执行快速回归，不执行可视化 E2E。

第十一期交付范围：

- 消息行增加发送者归属标记，稳定区分自己消息和他人消息。
- 消息操作区和回应区增加语义化分组。
- 消息操作按钮增加中文可访问标签和悬停说明。
- 自己消息的内容、操作区、回应区进一步右对齐。
- E2E 脚本补充消息归属和操作区语义断言。
- 开发期只执行快速回归，不执行可视化 E2E。

第十二期交付范围：

- 线程、文件、成员、审计面板增加统一标题区和统计信息。
- 线程根消息、回复列表、线程输入区保持独立层级，降低遮挡风险。
- 文件、成员、审计面板的加载态、空状态、错误态保留一致视觉位置。
- 右侧面板切换继续保持内容区独立滚动。
- E2E 脚本补充线程详情、文件、成员、审计面板标题断言。
- 开发期只执行快速回归，不执行可视化 E2E。

第十三期交付范围：

- 搜索结果增加频道消息、线程、私信类型标签。
- 搜索结果按类型增加左侧色条，提升扫描效率。
- 搜索结果按钮增加可访问名称。
- 未读和 `@我` 徽标统一尺寸与对齐。
- E2E 脚本补充搜索结果类型标签断言。
- 开发期只执行快速回归，不执行可视化 E2E。

第十四期交付范围：

- 窄屏侧栏高度使用动态区间，主区占用剩余视口。
- 输入区工具栏横向滚动更稳定，工具按钮不挤压输入框。
- 弹层在窄屏下使用近全屏布局。
- 管理后台移动端表单和策略区压缩间距。
- E2E 脚本补充窄屏侧栏高度区间断言。
- 开发期只执行快速回归，不执行可视化 E2E。

第十五期交付范围：

- 执行完整无头 E2E 阶段验收，14 个用例全部通过。
- 首次 E2E 发现线程回复按钮选择器过宽，已收窄到线程详情面板内定位。
- 刷新 `SMOKE_REPORT.html`、`TESTING.html`、`ROADMAP.html`、`ITERATION_PLAN.html`。
- 阶段 6.5 状态从“进行中”改为“已完成”。
- 阶段 6.5 后续进入阶段 7 企业系统集成，或回到阶段 6 后端生产化。

可感知 UI 优化补丁：

- 主聊天首屏增强频道标题、频道图标、安全协作信息条和置顶摘要，提升第一眼视觉变化。
- 消息列表增强背景层次、日期胶囊、气泡阴影、自己消息蓝色渐变、他人消息白色卡片和文件卡片质感。
- 输入区升级为工具栏、输入框、发送区的产品化结构，补充准备发送、输入消息后发送等辅助状态。
- 侧边栏增强品牌区、当前频道高亮条、搜索结果类型标签、未读和 `@我` 徽标。
- 右侧面板增强分段 Tab、统一标题卡片、线程、文件、成员和审计列表卡片质感。
- 补充无头 E2E 断言，覆盖频道信息条、输入区状态和侧边栏选中项。
- 收尾验收修复输入区状态文案优先级和侧边栏频道列表覆盖操作按钮问题。
- 完整无头 E2E 已重新通过，14 个用例全部通过。
- 本补丁不改变接口、数据库和权限逻辑。

验收：

- `node --check server/index.js`
- `node --check scripts/smoke.js`
- `node --check tests/e2e/beechat.regression.spec.js`
- `npm --prefix frontend run typecheck`
- `npm --prefix frontend run build`
- `npm run docs:build`
- `npm run smoke`
- `npm run e2e`
- `git diff --check`

## 十二、阶段 7：企业系统集成

状态：待启动。

目标：

- 让 BeeChat 成为企业内部系统的消息入口。

关键任务：

- 单点登录。
- LDAP / 企业微信 / 钉钉组织同步适配预留。
- Webhook。
- Bot 消息。
- 审批系统通知。
- 知识库消息引用。
- 外部系统回调鉴权。

核心交付物：

- SSO 接入文档。
- 组织同步接口。
- Webhook API。
- Bot API。

阶段验收：

- 外部系统可安全推送消息到 BeeChat。
- 企业用户身份来源可统一。

## 十三、阶段 8：平台化扩展

状态：规划中。

目标：

- 将 BeeChat 从聊天产品升级为企业消息与协同平台能力。

方向：

- 多工作区。
- 多租户隔离。
- 消息模板。
- 审批卡片。
- AI 助手接入。
- 机器人市场。
- 低代码通知编排。

阶段判断标准：

- 前 7 个阶段稳定后再启动。
- 已有真实业务系统接入需求。
- 已明确平台化商业或内部复用价值。

## 十四、近期里程碑

### M1：设计与文档闭环

目标日期：当前优先。

交付：

- 需求文档。
- 设计文档。
- 迭代计划。
- Roadmap。
- Pencil 正式设计源。

验收：

- 文档索引完整。
- 设计文件可打开。
- 冒烟测试通过。

### M2：视觉验收与 UI 收口

交付：

- 浏览器截图。
- 桌面端视觉修复。
- 窄屏布局修复。
- 发现频道交互确认。

验收：

- 主聊天页、登录页、线程页、发现频道无明显错位。

### M3：前端工程化方案

交付：

- 前端技术选型。
- 目录结构方案。
- 组件拆分方案。
- 迁移步骤。

验收：

- 可按模块逐步迁移，不阻断现有 MVP。

## 十五、优先级排序

```text
P0  主聊天工作台 UI/UX 打磨
P0  ChatWorkspace 结构拆分
P0  开发期快速回归与 HTML 报告
P1  侧边栏与搜索体验
P1  管理后台与审计详情
P1  权限模型细化
P2  PostgreSQL / Redis / 对象存储
P2  SSO / 组织同步 / Webhook
P3  平台化与 AI 能力
```

## 十六、关键决策点

近期需要确认：

1. 前端工程化选择 React 还是 Vue。
2. 是否保留当前 Node.js 原生后端，还是后续迁移到 Spring Boot。
3. 生产数据库优先 PostgreSQL 还是 MySQL。
4. 文件存储使用本地磁盘、MinIO 还是其他对象存储。
5. 审计权限是否仅管理员可见。
6. BeeChat 是否作为 BeePilot / BeeWorks 后续消息中心底座。
