# BeeChat 迭代计划

## 一、当前进展

当前 BeeChat 已完成 MVP 底座、第一轮设计同步与前端工程化主体改造，状态如下：

```text
产品方向       企业内网私有化协同聊天
工程形态       Node.js 原生服务 + Vite React 前端 + SQLite
设计源文件     Pencil 多画板已重建
当前主线       阶段 6.5 主聊天体验打磨与前端结构收敛
核心验证       开发期快速回归通过后更新 HTML 报告
```

## 二、已完成阶段

### 阶段 0：项目纠偏与目录归档

状态：已完成。

- 确认 BeeChat 独立目录为 `bee-chat/`。
- 将 BeeChat 文档统一放入 `docs/beechat/`。
- 将 BeePilot 文档统一放入 `docs/beepilot/`。
- 将设计文件统一放入 `design/beechat/`。
- 识别并隔离旧 Pencil 占位文件。

### 阶段 1：MVP 功能底座

状态：已完成。

- 登录与会话恢复。
- 频道、私信、消息、线程。
- 文件上传与下载。
- 搜索、未读数、WebSocket 实时刷新。
- 审计、敏感词提示。
- 成员管理与基础权限。
- SQLite 持久化。
- 数据库初始化迁移。
- Docker 与本地启动脚本。
- 冒烟测试脚本。

### 阶段 2：设计重建与前端同步

状态：主体已完成，进入验收收口。

- 重建 `beechat-main-chat.pen`。
- 补充 `DESIGN_SPEC.md`。
- 前端同步新版视觉风格。
- 新增发现频道弹层。
- 补充前端动态内容 HTML 转义。
- 已补充一屏布局约束与回归用例。
- 待完成：阶段验收截图归档、移动端视觉细节继续微调。

### 阶段 3：前端工程化

状态：主体已完成，进入持续完善。

目标：将当前原生前端升级为可长期维护的工程化前端。

已采用技术路线：

```text
Vite + React + TypeScript
组件化业务模块
统一 API Client
统一设计令牌
```

已完成：

- 拆分登录页、主工作台、侧栏、消息列表、线程面板、审计面板、成员面板。
- 抽象 Avatar、Badge、Button、EmptyState、Modal 等基础组件。
- 引入统一主题变量，与 `DESIGN_SPEC.md` 对齐。
- 增加前端构建脚本与静态资源输出目录。
- 增加 Playwright 回归脚本与基础页面级验证。

待收口：

- 继续拆分 `ChatWorkspace` 内部业务组件。
- 设置页第一期已实现，支持个人资料、通知偏好、快捷键展示与退出登录入口。
- 补充更多可访问性与响应式断言。

验收：

- 功能与当前 MVP 等价。
- 视觉与 Pencil 主画板一致。
- `npm run smoke` 继续通过。

## 三、当前插入阶段：阶段 6.5 主聊天体验打磨

状态：已完成。第十五期阶段验收已通过，14 个无头 E2E 用例全部通过。

插入原因：

- 后端生产化已推进到阶段 6 第十五期，继续深挖前先把主聊天体验打磨稳定。
- `ChatWorkspace` 和 `ChatWorkspace.css` 已膨胀，后续继续改 UI 的风险偏高。
- E2E 已覆盖主流程，但 UI/UX 细节断言不足。

### 第一期：主聊天界面打磨

本期交付：

- 新增 `ChatMainPanel`，承接频道头部、提示区、消息列表和输入区。
- `ChatWorkspace` 保留状态编排、数据刷新和事件分发。
- 新增 `useWorkspaceSearch`、`useDraftComposer`、`useThreadWorkspace`、`useDiscoverChannels`、`useSettingsWorkspace`、`useAdminWorkspace`。
- `ChatWorkspace.tsx` 已从 737 行收敛到 500 行以内。
- 主工作台继续保持一屏内完成核心操作。
- 消息长文本、文件卡片、线程根消息增加换行保护。
- 消息操作按钮常态弱化，悬停和聚焦时增强。
- 输入区固定底部，发送、上传、草稿状态层级更清晰。
- 右侧面板 Tab 固定顶部，内容区独立滚动。
- 私信消息不展示线程入口。
- 增强 Playwright 无头用例：一屏布局、长文本、输入区、右侧面板切换。
- 更新 `DESIGN.md` React 组件映射。

快速回归：

```text
node --check server/index.js
node --check scripts/smoke.js
node --check tests/e2e/beechat.regression.spec.js
npm --prefix frontend run typecheck
npm --prefix frontend run build
npm run docs:build
npm run smoke
git diff --check
```

### 第二期：侧边栏与搜索体验

交付：

- 频道、私信、搜索结果密度优化。已完成：频道与私信统一 `sidebar-nav-item` 结构。
- 未读、`@我`、在线状态标识统一。已完成：频道未读、`@我`、私信未读统一徽标结构。
- 搜索结果空状态、加载态、错误态补齐。已完成：搜索标题、结果数量、空状态、错误态、加载态和悬停聚焦反馈。
- 搜索定位后的高亮与滚动体验继续优化。已完成：搜索结果区域独立滚动，不撑破侧边栏。
- 快速回归补充。已完成：侧边栏搜索、频道 `@我`、私信项和搜索布局断言。

### 第三期：管理后台 UI 收敛

交付：

- 管理后台表单分区优化。已完成：管理后台增加摘要头、指标区、用户区、策略区、频道区、审计区。
- 审计筛选体验优化。已完成：审计详情显示最近记录数量，保留独立滚动。
- 角色、安全策略、文件策略、网络策略视觉层级优化。已完成：策略表单归入三列策略网格，窄屏自动单列。
- 审计导出入口增强。已完成：导出 CSV 提升为管理后台顶部主操作。
- 快速回归补充。已完成：管理后台打开后校验摘要区、策略区和面板高度不溢出。

### 第四期：窄屏适配与阶段验收

交付：

- 窄屏侧栏、主消息区、右侧面板适配。已完成：侧栏内部滚动、主区输入区固定、右侧面板窄屏隐藏。
- 完整无头 E2E 阶段验收。已完成：`npm run e2e`，14 个用例全部通过。
- 用户要求观看过程时再执行可视化 E2E。已遵守：本次未执行可视化 E2E。
- 归档 HTML 测试报告。已完成：Playwright 报告与 BeeChat HTML 文档均已生成。

### 第五期：主消息区状态与输入区反馈

交付：

- 消息加载错误态增加明确重试按钮，失败后不需要刷新整页。
- 发送失败、上传失败从消息列表错误态拆出，固定展示在输入区上方。
- 输入区发送按钮同时响应空内容、发送中、上传中状态。
- 输入区状态徽标补充“待输入”“上传中”，降低用户对禁用按钮的困惑。
- 保持主工作台一屏布局约束，继续不新增大型后端能力。

### 第六期：主聊天区 CSS 结构收敛

交付：

- 新增 `ChatMainPanel.css`，承接频道头部、安全提示、频道公告样式。
- 新增 `MessageList.css`，承接消息列表、消息气泡、文件卡片、反应区和消息错误态样式。
- 新增 `MessageComposer.css`，承接输入区、附件入口、@ 人快捷入口和输入区错误态样式。
- `ChatWorkspace.css` 从 1421 行降到 1056 行，继续保留工作台布局、侧边栏、右侧面板、弹层和管理后台样式。
- 本期不改变 DOM 结构和接口语义，降低视觉回归风险。

### 第七期：侧边栏 CSS 结构收敛

交付：

- 新增 `ChatSidebar.css`，承接品牌、全局搜索、搜索结果、频道、私信、当前用户卡片样式。
- `ChatWorkspace.css` 从 1056 行继续降到 796 行，只保留工作台网格、主区壳、右侧面板、弹层和管理后台样式。
- 侧边栏搜索、频道未读、`@我`、私信在线状态仍沿用原 DOM 与测试定位。
- 本期不改变接口语义，不新增后端能力。

### 第八期：右侧面板 CSS 结构收敛

交付：

- 新增 `RightPanel.css`，承接右侧面板壳层、线程、成员、文件和审计面板样式。
- `RightPanel.tsx` 独立引入右侧面板样式，继续保持 Tab 壳层职责。
- `ChatWorkspace.css` 从 796 行继续降到 474 行，只保留工作台网格、主区壳、弹层和管理后台样式。
- 线程根消息、回复列表、成员列表、文件卡片、审计筛选仍沿用原 DOM 与测试定位。
- 本期不改变接口语义，不新增后端能力。

### 第九期：弹层与管理后台 CSS 结构收敛

交付：

- 新增 `DiscoverChannelsModal.css`，承接发现频道创建表单、频道列表和频道卡片样式。
- 新增 `SettingsModal.css`，承接设置页个人资料、偏好开关、快捷键和操作区样式。
- 新增 `AdminModal.css`，承接管理后台摘要、指标、用户、频道、审计和策略区样式。
- `ChatWorkspace.css` 从 474 行继续降到 92 行，只保留工作台网格、主区壳和窄屏工作台布局规则。
- 发现频道、设置、管理后台仍沿用原 DOM 与测试定位。
- 本期不改变接口语义，不新增后端能力。

### 第十期：输入区交互状态打磨

交付：

- `MessageComposer` 增加 `empty`、`ready`、`sending`、`uploading` 状态标记，便于 UI 与测试稳定识别。
- 输入区表单增加 `aria-busy`，状态栏增加 `aria-live`，提升辅助访问体验。
- 上传入口改为固定尺寸工具按钮，增加 `composer-upload-button` 测试定位。
- 发送或上传过程中锁定输入框，避免重复操作导致状态错乱。
- E2E 脚本补充输入区空态、就绪态和上传按钮可用性断言。
- 本期不改变接口语义，不新增后端能力。

### 第十一期：消息列表阅读与操作语义增强

交付：

- `MessageList` 消息行增加 `data-message-owner`，稳定区分自己消息和他人消息。
- 消息行增加可访问名称，辅助识别消息归属。
- 消息操作区和回应区增加 `role="group"` 与中文语义标签。
- 消息操作按钮增加 `aria-label` 与 `title`，提升键盘和读屏可用性。
- 自己消息的气泡、操作区、回应区进一步右对齐，保持阅读结构稳定。
- E2E 脚本补充自己消息归属、消息操作组和回应组断言。
- 本期不改变接口语义，不新增后端能力。

### 第十二期：线程与右侧面板体验精修

交付：

- `ThreadPanel` 增加统一面板头，展示线程回复数量和加载状态。
- `FilePanel` 增加统一面板头，展示频道文件数量和排序说明。
- `MemberPanel` 增加统一面板头，展示频道成员数量和管理权限状态。
- `AuditPanel` 增加统一面板头，展示审计记录数量和筛选能力。
- 线程区改为独立纵向布局，根消息、提醒、回复列表、输入区层级更稳定。
- E2E 脚本补充线程详情、成员、文件、审计面板标题断言。
- 本期不改变接口语义，不新增后端能力。

### 第十三期：侧边栏与搜索体验精修

交付：

- 搜索结果增加类型标签，区分频道消息、线程和私信。
- 搜索结果按类型增加左侧色条，提升扫描效率。
- 搜索结果按钮增加中文可访问名称，便于键盘和读屏识别。
- 未读和 `@我` 徽标统一最小宽度、外边距和内边距。
- E2E 脚本补充搜索结果类型标签断言。
- 本期不改变接口语义，不新增后端能力。

### 第十四期：移动端与窄屏细节精修

交付：

- 窄屏侧栏高度改为 `clamp(210px, 32dvh, 280px)`，主区使用剩余视口高度。
- 输入区工具栏增强横向滚动，工具按钮固定宽度，避免挤压输入框。
- 弹层在窄屏下改为距离视口 8px 的近全屏布局。
- 管理后台移动端表单和策略区压缩间距，提升可操作面积。
- E2E 脚本补充窄屏侧栏高度区间断言。
- 本期不改变接口语义，不新增后端能力。

### 第十五期：阶段验收与归档

交付：

- 执行完整无头 E2E 阶段验收，14 个用例全部通过。
- 修复阶段验收中暴露的线程回复按钮选择器歧义。
- 刷新 HTML 文档和冒烟报告。
- 阶段 6.5 状态改为已完成。
- 后续建议进入阶段 7 企业系统集成，或回到阶段 6 后端生产化。

### 补丁：可感知 UI 优化

状态：已完成。

交付：

- 主聊天首屏增强频道标题、频道图标、安全协作信息条和置顶摘要，打开页面即可看到层级变化。
- 消息列表增强背景层次、日期胶囊、自己消息蓝色气泡、他人消息白色气泡、状态标签和文件卡片质感。
- 输入区升级为明确的工具栏、输入框和发送区结构，增加准备发送、输入消息后发送等辅助状态。
- 侧边栏增强品牌区、当前频道选中态、搜索结果类型标签、未读和 `@我` 徽标。
- 右侧面板增强分段 Tab、面板标题卡片、线程根消息、回复列表、文件、成员和审计卡片质感。
- E2E 脚本补充频道信息条、输入区状态和侧边栏选中项断言。
- 收尾验收已修复输入区状态文案优先级和侧边栏频道列表覆盖操作按钮问题。
- 完整无头 E2E 已重新通过，14 个用例全部通过。
- 本补丁不改变接口语义、数据库结构和权限逻辑。

## 四、后续阶段计划

### 阶段 4：企业安全与管理

状态：已验收，审计组合筛选、文件上传策略、网络策略、审计员角色与频道管理员角色已完成。

目标：补齐私有化企业协同所需的基础管理能力。

第一期已完成：

- 管理员专属入口。
- 管理后台弹层。
- 用户列表与角色、在线状态展示。
- 频道列表与频道说明维护。
- 最近审计详情展示。
- `/api/admin/overview` 管理概览接口。
- `/api/admin/channels/:id` 频道说明更新接口。

第二期已完成：

- 角色权限配置。
- 审计导出。
- 密码策略。
- 登录失败锁定。

后续任务：

- IP 白名单。
- 更细粒度角色权限矩阵。

第三期已完成：

- 审计按类型、操作者、关键词、起止日期组合筛选。
- 审计组合筛选快速回归覆盖。
- 文件上传大小与扩展名策略配置。
- 文件上传策略快速回归覆盖。
- 新增审计员角色。
- 普通成员审计访问收敛。
- 审计员可查看与导出审计，但不可进入管理总览。
- 新增频道管理员角色。
- 频道管理员可维护已加入频道的成员与频道说明。
- 频道管理员不可访问管理总览、安全策略、文件策略和用户角色配置。
- 新增 IP 白名单网络策略。
- 网络策略默认关闭，开启后统一拦截非白名单 IP。

验收：

- 管理员与普通用户权限边界清晰。
- 审计可按时间、操作者、类型过滤。
- 高风险操作有审计记录。
- `npm run e2e` 阶段验收通过，11 个用例全部通过。

### 阶段 5：消息与协作增强

状态：主体完成，第一期已完成频道公告与置顶消息，第二期已完成消息收藏与表情回应，第三期已完成图片预览与频道文件列表，第四期已完成 @ 人提醒与本地草稿，第五期已完成搜索结果定位，第六期已完成在线状态优化。

目标：提升日常协同效率。

任务：

- @ 人提醒。已完成：发送消息识别 `@姓名`、频道提及未读、侧栏提及标识、消息区 `@我` 标识、快速回归。
- 频道公告。已完成：频道模型、管理维护、顶部展示、审计记录、快速回归。
- 置顶消息。已完成：置顶接口、权限控制、消息区标识、置顶摘要、审计记录、快速回归。
- 消息收藏。已完成：收藏接口、个人收藏态、消息标识、审计记录、快速回归。
- 图片预览。已完成：图片文件消息缩略预览、下载入口复用、快速回归。
- 文件列表。已完成：频道文件列表接口、右侧文件面板、图片与普通文件区分展示、快速回归。
- 搜索结果定位到消息上下文。已完成：侧栏搜索、结果列表、频道切换、目标消息高亮、快速回归。
- 草稿。已完成：频道维度本地草稿保存、切换频道恢复草稿、发送后清理草稿、输入区保存提示。
- 表情回应。已完成：快捷回应接口、回应计数、个人回应态、审计记录、快速回归。
- 在线状态优化。已完成：基于有效会话与实时连接动态计算在线状态，退出登录后释放在线状态，WebSocket 推送 `presence:updated`，快速回归覆盖。

验收：

- 频道协作流程更接近真实企业使用。
- 搜索、文件、线程能形成完整闭环。

### 阶段 6：生产级部署架构

状态：进行中，第一期已完成健康检查增强、运行指标接口和本地备份脚本，第二期已完成结构化日志与日志落盘，第三期已完成生产部署配置骨架，第四期已完成 PostgreSQL 适配计划与表结构迁移骨架，第五期已完成 PostgreSQL 一次性导入 SQL 生成脚本，第六期已完成数据库运行模式保护，第七期已完成 PostgreSQL 查询适配层第一版，第八期已完成业务仓储契约第一版，第九期已完成主服务低风险只读路径接入 SQLite 仓储，第十期已完成频道消息分页只读路径仓储接入，第十一期已完成私信分页只读路径仓储接入，第十二期已完成审计查询只读路径仓储接入，第十三期已完成 PostgreSQL 频道文件列表只读查询，第十四期已完成频道消息与线程回复发送写入路径接入 SQLite 仓储，第十五期已完成私信发送写入路径接入 SQLite 仓储，第十六期已完成消息编辑与消息撤回写入路径接入 SQLite 仓储。

目标：从 MVP 单机形态升级为可生产部署形态。

任务：

- PostgreSQL 适配。已完成第十六期：PostgreSQL 初始化迁移、生产环境变量约定、SQLite 到 PostgreSQL 迁移路径、一次性导入 SQL 生成脚本、数据库运行模式保护、查询适配层、业务仓储契约、主服务低风险只读路径仓储接入、频道消息分页仓储接入、私信分页仓储接入、频道文件列表 PostgreSQL 查询、审计查询仓储接入、频道消息发送写入 SQLite 仓储接入、私信发送写入 SQLite 仓储接入、消息编辑与消息撤回写入 SQLite 仓储接入、PostgreSQL 消息写入显式未启用保护、回滚策略和静态检查脚本。
- Redis 会话与 WebSocket 分布式广播。
- 对象存储适配。
- 反向代理配置。已完成第三期：`deploy/nginx/beechat.conf` 提供 HTTP 代理和 WebSocket 升级样例。
- 生产 Compose。已完成第三期：`docker-compose.prod.yml`、`.env.prod.example`、数据卷、日志卷、备份卷、健康检查和容器日志轮转。
- 备份与恢复。已完成第一期：`npm run backup` 备份 SQLite、JSON 快照、上传文件和清单。
- 健康检查与监控指标。已完成第一期：`/api/health` 返回启动时间与运行时长，`/api/metrics` 返回进程、内存、会话、实时连接、数据大小、业务计数和迁移状态。
- 日志分级与审计日志落盘。已完成第二期：JSON Lines 文件日志、HTTP 访问日志、异常日志、审计日志、启动日志、迁移日志和 5MB 自动轮转。
- 数据库迁移版本管理增强。

第十六期完成：

- 消息编辑与消息撤回写入链路已迁移到仓储。
- SQLite 已补齐消息内容编辑与撤回状态变更仓储方法。
- PostgreSQL 已为消息编辑、撤回写入预留显式未启用保护。
- 仓储契约检查、冒烟静态检查和快速回归已覆盖新增链路。

第十七期建议：

- 迁移文件上传发送链路到仓储，收敛剩余消息写入路径。
- 补齐 PostgreSQL 文件消息写入保护与后续真实库实现计划。
- 继续保持开发期快速回归，阶段验收再运行完整 E2E。

验收：

- 支持单机私有化部署。
- 支持后续平滑扩展到多实例。
- 数据备份与恢复流程可演练。

### 阶段 7：企业集成

目标：接入企业已有身份、组织和办公系统。

任务：

- 单点登录。
- LDAP / 企业微信 / 钉钉组织同步适配预留。
- Webhook。
- Bot 消息入口。
- 内部知识库关联。
- 审批系统消息通知。

验收：

- BeeChat 可作为企业内部消息中心底座。
- 外部系统接入有清晰接口与鉴权策略。

## 五、优先级建议

近期优先级：

```text
P0  浏览器视觉验收与 UI 细节修复
P0  文档补齐与仓库整理
P1  前端工程化组件细拆
P1  管理后台增强与审计导出
P2  PostgreSQL / Redis / 对象存储
P2  企业组织与单点登录
```

## 六、风险与处理

| 风险 | 影响 | 处理方式 |
| --- | --- | --- |
| 原 Pencil 源文件丢失 | 设计追溯困难 | 已重建正式 Pencil 源文件 |
| 原生前端继续膨胀 | 后期维护成本升高 | 阶段 3 迁移工程化前端 |
| SQLite 不适合多人生产 | 数据一致性与扩展受限 | 阶段 6 引入 PostgreSQL |
| WebSocket 单实例限制 | 多实例无法广播 | 阶段 6 引入 Redis Pub/Sub |
| 审计权限边界不够细 | 合规风险 | 阶段 4 收敛审计权限 |

## 七、当前建议执行项

1. 完成浏览器视觉验收与截图归档。
2. 清理并提交当前文档、设计、前端工程化改动。
3. 继续拆分 `ChatWorkspace`，降低单文件复杂度。
4. 启动阶段 4 管理后台与审计详情设计。
